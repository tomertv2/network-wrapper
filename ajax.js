// // todo later: move common functionality from all CRUD functions to a single function

// function create(binName, binData) { // todo: use await
//   return network.post({binName}, binData);
// }

// function read(binId) {
//   return network.get({binId})
// }
  
// function update(binId, binData) {
//   return network.put({binId}, binData);
// }

// function destroy(binId) {
//   return network.delete({binId});
// }

// // todo later: use https://pokeapi.co/ to import a pokemon json by pokemon name
